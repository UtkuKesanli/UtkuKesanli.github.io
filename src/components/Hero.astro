<section class="hero" id="home">
  <div class="glow" aria-hidden="true"></div>

  <div class="hero-topline">
    <span>Portfolio / 2026</span>
    <span>Izmir, TR</span>
  </div>

  <div class="hero-copy">
    <p class="eyebrow">Flutter Developer · Mobile Product Builder</p>
    <h1>
      <span class="line">UTKU</span>
      <span class="line outline">KEŞANLI</span>
    </h1>

    <div class="hero-bottom">
      <p class="intro">
        I design and build mobile products from first idea to release —
        combining Flutter engineering, Firebase and product-focused UI/UX.
      </p>

      <a class="projects-link" href="#projects">
        <span>View projects</span>
        <span class="arrow" aria-hidden="true">↓</span>
      </a>
    </div>
  </div>

  <div class="scroll-note" aria-hidden="true">SCROLL TO EXPLORE</div>
</section>

<script>
  const hero = document.querySelector('.hero');
  const glow = document.querySelector('.glow');

  if (hero instanceof HTMLElement && glow instanceof HTMLElement) {
    hero.addEventListener('pointermove', (event) => {
      const bounds = hero.getBoundingClientRect();
      const x = event.clientX - bounds.left;
      const y = event.clientY - bounds.top;

      glow.style.setProperty('--pointer-x', `${x}px`);
      glow.style.setProperty('--pointer-y', `${y}px`);
    });
  }
</script>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 2rem clamp(1.5rem, 5vw, 5rem) 2.5rem;
    isolation: isolate;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -3;
    opacity: 0.28;
    background-image:
      linear-gradient(var(--line) 1px, transparent 1px),
      linear-gradient(90deg, var(--line) 1px, transparent 1px);
    background-size: 72px 72px;
    mask-image: linear-gradient(to bottom, black, transparent 82%);
  }

  .glow {
    --pointer-x: 65%;
    --pointer-y: 35%;
    position: absolute;
    inset: 0;
    z-index: -2;
    background: radial-gradient(
      600px circle at var(--pointer-x) var(--pointer-y),
      rgba(143, 124, 255, 0.18),
      transparent 55%
    );
    transition: background 80ms linear;
  }

  .hero-topline {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    color: var(--text-muted);
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    animation: reveal 900ms var(--ease-out) both;
  }

  .hero-copy {
    width: min(100%, var(--max-width));
    margin: auto;
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: var(--accent);
    font-size: clamp(0.72rem, 1.1vw, 0.9rem);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    animation: reveal 900ms 120ms var(--ease-out) both;
  }

  h1 {
    margin: 0;
    font-size: clamp(5rem, 14vw, 13rem);
    line-height: 0.76;
    letter-spacing: -0.075em;
    font-weight: 800;
  }

  .line {
    display: block;
    animation: title-reveal 1100ms 140ms var(--ease-out) both;
  }

  .line.outline {
    color: transparent;
    -webkit-text-stroke: 1.5px var(--text);
    animation-delay: 220ms;
  }

  .hero-bottom {
    margin-top: clamp(2.5rem, 6vw, 5rem);
    display: grid;
    grid-template-columns: minmax(0, 620px) auto;
    align-items: end;
    justify-content: space-between;
    gap: 3rem;
    animation: reveal 900ms 400ms var(--ease-out) both;
  }

  .intro {
    margin: 0;
    color: var(--text-muted);
    font-size: clamp(1rem, 1.6vw, 1.35rem);
    line-height: 1.6;
  }

  .projects-link {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 1.15rem 1rem 1.4rem;
    border: 1px solid var(--line);
    border-radius: 999px;
    white-space: nowrap;
    transition: background 220ms ease, border-color 220ms ease, transform 220ms ease;
  }

  .projects-link:hover {
    border-color: var(--accent);
    background: var(--accent-soft);
    transform: translateY(-3px);
  }

  .arrow {
    display: grid;
    place-items: center;
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    background: var(--accent);
    color: #0a0a0a;
    transition: transform 220ms ease;
  }

  .projects-link:hover .arrow {
    transform: translateY(3px);
  }

  .scroll-note {
    color: var(--text-muted);
    font-size: 0.64rem;
    letter-spacing: 0.2em;
    text-align: right;
  }

  @keyframes reveal {
    from {
      opacity: 0;
      transform: translateY(16px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes title-reveal {
    from {
      opacity: 0;
      transform: translateY(60px) skewY(4deg);
      filter: blur(8px);
    }
    to {
      opacity: 1;
      transform: translateY(0) skewY(0);
      filter: blur(0);
    }
  }

  @media (max-width: 860px) {
    .hero {
      min-height: 100svh;
      padding: 1.25rem 1.1rem 1.5rem;
    }

    .hero-topline span:last-child {
      display: none;
    }

    h1 {
      font-size: clamp(4.1rem, 21vw, 7rem);
      line-height: 0.82;
    }

    .hero-bottom {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .projects-link {
      width: max-content;
    }

    .scroll-note {
      display: none;
    }
  }
</style>
